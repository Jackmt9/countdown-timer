import { createUser, loginUser, deleteUser, logoutUser, stayLoggedIn, updateUserInfo} from '../actions/UserActions'

class UserContainer extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default UserContainer;